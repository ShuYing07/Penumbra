# 合规审计指南（COMPLIANCE_GUIDE）

本工具严格定位为"客观数据展示与统计分析工具"，不构成任何投资建议。本文说明合规审计追踪如何工作、如何查看与导出。

## 一、三层合规防护

1. **入口拦截**：程序启动弹出强制免责声明确认，必须勾选才能进入。
2. **过程拦截**：所有 AI 输出经过 `core/compliance.py` 的 `sanitize_ai_output`，命中"买入/卖出/推荐/目标价/必涨"等词时替换为【已过滤】，并自动追加免责声明。
3. **审计留痕**：每次分析写入 `core/audit_trail.py`（`data/audit_trail.db`）。

## 二、审计记录内容

`audit_trail` 表记录：
- 输入数据摘要（截断至 500 字，脱敏）
- 使用的模型名称
- System Prompt 的 SHA-256 指纹（不存原文）
- 过滤结果：通过 / 部分过滤 / 拦截
- 命中的禁用词明细
- 人工处理动作（拦截/查看原文/标记误报）

## 三、人工干预节点

当合规过滤检测到命中词时：
- **拦截**：替换为【已过滤】，不展示原文；
- **查看原文**：可见原文但标记为"未过滤"；
- **标记为误报**：记录到审计日志后放行。

该决策会写入 `manual_action` 字段，事后可追溯。

## 四、查看与导出

- 在程序「分析日志」标签页查看历史 AI 分析记录，可导出 CSV/JSON；
- 审计日志通过 `core/audit_trail.list_audit()` / `export_audit()` 读取；
- 所有数据库位于本机 `data/` 目录，不联网上传。

## 五、法律定位

本软件不提供证券投资咨询业务，未取得中国证监会证券投资咨询业务资格。
证券投资有高风险，历史回测不代表未来收益。用户因实盘操作产生的一切盈亏与法律后果，均由用户自行承担。
