# 操作日志（只追加，最新在最下；格式：时间 · 动作 · 结果/结论）

## 2026-09-10 · 项目启动：现状盘点 + 开源调研 + 需求对齐 + 知识库初建
- 接收任务：开发可独立运行的虚拟股票预测 exe（联网取资讯+AI 分析推演+炒股方案），要求先出计划书、调研 GitHub/X 同类项目、确认后再编码；全程记录形成 D 盘知识库。
- 加载 TRAE-plan-mode 技能（调研→计划→批准→执行 工作流）。
- 现状盘点：工作目录 d:\craecode\magic 为空；搜索发现既有脚手架 `D:\StockAIPredictor\`（仅 venv + requirements.txt + .env + .gitignore，无业务代码）。
  - venv = Python 3.14.7；依赖已装齐：pandas 3.0.5 / numpy 2.5.3 / akshare 1.18.94 / yfinance 1.7.0 / langchain 1.4 / langgraph 1.2 / chromadb 1.5.9 / openai 3.11 / pydantic 2.13 / PyQt6 6.11 / pyqtgraph 0.14 / matplotlib 3.11。
  - `.env` 已有 `DEEPSEEK_API_KEY`（敏感，不回显不外传；.gitignore 已覆盖）。
- GitHub API 直连成功（无需代理）调研 8 个项目：TradingAgents 104.4k★(Apache-2.0, 09-07 活跃)、OpenBB 72.9k★(许可证 NOASSERTION)、ai-hedge-fund 63.3k★(MIT, 09-03 活跃)、qlib 48.5k★(MIT)、yfinance 25.2k★、akshare 22.5k★、FinGPT 21.2k★、FinRL 16.3k★。
- 深读两标杆 README + 目录结构：
  - TradingAgents：LangGraph 多智能体（技术/基本面/新闻/情绪分析师→多空辩论→交易员→风控→组合经理）；决策日志+事后收益反思；checkpoint 续跑；DeepSeek 官方支持；yfinance 覆盖美股/港股/A股(.SS/.SZ)/加密；v0.4.0 修过全链路前视偏差；Windows UTF-8 修复；代理支持。
  - ai-hedge-fund：基金(mandate)一等实体、alpha 模型可插拔可回测、模块切分(data/features/signals/strategies/risk/portfolio/backtesting/validation/tui)；数据依赖付费 Financial Datasets API（不照搬）。
- WebSearch 核实两件关键事实：
  - PyInstaller 6.15.0(2025-08) 起支持 Python 3.14，现最新 6.22.x 支持 3.8~3.15；3.14.4 forkserver 打包 bug 已于 6.20 修复 → 不用降 Python。
  - DeepSeek 现役模型 `deepseek-flash`(V4.1-Flash) / `deepseek-v4-pro`(V4-Pro-0813)；旧 deepseek-chat/deepseek-reasoner 2026-07-24 弃用且报错无跳转；1M 上下文、支持 JSON/tool calls；flash 约 1~2 元/百万输入、4~8 元/百万输出（高峰），空闲半价；单次多智能体分析估约 0.1~0.3 元。
- AskUserQuestion 四问，用户拍板：① 市场=A股+美股+加密货币都要；② AI=DeepSeek 云端为主；③ 界面=PyQt6 原生桌面窗口；④ 定位=学习研究+模拟盘。
- 建立本知识库 D:\StockAIPredictor\docs\kb\：00 总索引/01 开源调研/02 技术决策/03 踩坑台账/本日志，完成首次全量沉淀。
- 下一步：撰写项目计划书（.trae/documents/虚拟股票预测程序_plan.md）→ NotifyUser 请用户确认 → 批准后按 5 期顺序编码。

## 2026-09-10 · 计划书获批 + 一期编码（数据/指标/LLM/智能体主体）
- 计划书《虚拟股票预测程序_plan.md》完成并经用户批准，五期：一期数据+多智能体 CLI → 二期 PyQt6 界面 → 三期模拟盘+回测+反思 → 四期 RAG 学习库 → 五期打包交付。
- 一期代码主体落地（项目根 D:\StockAIPredictor\）：
  - core/config.py：.env/路径/模型名/代理双上下文（domestic_network 强制 NO_PROXY=*；foreign_network 注入 127.0.0.1:7897）/UTF-8 日志/成本估算/DISCLAIMER。
  - core/data：cache.py（SQLite point-in-time：daily_bars/news/token_ledger，INSERT OR IGNORE 永不覆盖）；akshare_source.py（新浪日线优先→腾讯→东财末位；hq.sinajs.cn 实时报价 GBK+Referer；stock_news_em 新闻；F10 资料）；yfinance_source.py（美股/加密日线 2y、fast_info 实时、info 基本面、news 新旧结构兼容，已处理多级列/tz/去重）；news_sources.py（财联社 stock_info_global_cls + Google News RSS）；service.py 门面（market_of 识别 SH/SZ=CN、XXX-USD=CRYPTO、纯字母=US；缓存优先→联网增量→失败回退缓存）。
  - core/quant/indicators.py：sma/ema/macd(DIF,DEA,柱=2×差)/rsi(Wilder)/kdj(9,3,3 国内递推初值50)/boll(ddof=0)/atr + latest_snapshot 技术事实 dict。
  - core/llm.py：LLMRunner（response_format=json_object、剥离代码块、失败重试2次、402 立即抛错并设闩锁短路后续调用、逐节点 token 记账；STOCKAI_MOCK=1 离线桩）。实测真实调用：key 有效但 **DeepSeek 余额不足返回 402**，待用户充值；mock 模式保证开发不阻塞。
  - core/agents：schemas.py（AnalystSignal/Debate/TraderPlan/Scenario/RiskDecision，各带 degraded）、prompts.py（数字程序算好喂模型的 price grounding 纪律）、nodes.py（技术/基本面/新闻/情绪→多头/空头→交易员→风控 8 节点线性图；单标的仓位上限 A股30%/美股25%/加密20%）。
- 连通性结论：yfinance 直连就通（Clash TUN 接管）；东财 push2 系域 443 在本机环境不可用（详见踩坑台账#3）；新浪日线 600519 0.9s/6002 行；腾讯 10.3s；stock_news_em 与财联社正常。

## 2026-09-10 · 一期收尾：LangGraph 编排 + 报告 + CLI + 三标的验收 + 打包冒烟
- 新增 core/agents/graph.py：AnalysisState(TypedDict) + gather_facts（行情→指标→实时→基本面→新闻）+ 8 节点线性 LangGraph（MemorySaver）+ `_to_native` 递归清洗 numpy 类型 + run_analysis 入口。
- 新增 core/memory/decision_log.py：decisions 表落每次决策完整快照（final/signals/trader/risk/tokens，预留 evaluated/realized_return_pct/reflection 供三期反思）；list_recent 查询。
- 新增 core/report.py：九段式中文 Markdown 报告（结论/情景推演表/操作方案/依据/风险/四分析师观点/多空辩论/技术事实/基本面/资讯+token 成本+免责声明），指标 dict 渲染为 key=value 行。
- 新增 run_analysis.py CLI：`run_analysis.py [tickers...] [--mock] [--model]`，报告写 data/reports/，.gitignore 补 build/ 与 data/。
- 指标单测 tests/test_indicators.py：**8/8 通过**（常量序列、全涨 RSI=100、全跌=0、布林轨道、KDJ 手工值 K=66.6667、ATR、snapshot 结构）。
- 修复三处问题：① tech.atr14_pct 残留 numpy.float64 导致 MemorySaver msgpack 序列化崩溃（加 _to_native + float 强转）；② mock 多空节点键名 debate_bull/debate_bear 未匹配（startswith 修复）；③ 报告 None/原始 dict 展示美化。
- **一期验收**：STOCKAI_MOCK=1 对 SH600519、AAPL、BTC-USD 三标的完整跑通（数据采集→8 节点→决策落库→Markdown 报告）；真实 DeepSeek 路径验证 402 时 8 节点全部优雅降级、报告照常产出、退出码 0；新增 402 闩锁后一次余额不足即短路不再重复请求。待用户充值后用真实 LLM 复验一次即闭环。
- **打包冒烟**：venv 安装 PyInstaller 6.22.2；scripts/pack_smoke.py（PyQt6 窗口 + --selftest 1 秒自检）；onedir 打包 build/dist/smoke（91.4 MB），exe --selftest 退出码 0、无残留进程。Python 3.14.7 打包链路确认可用。
- 一期状态：核心链路全部完成；遗留小项=A 股 F10 基本面源（二期补雪球/新浪）、真实 LLM 复验（待充值）、全量业务 exe 打包（五期）。下一步按计划进入二期 PyQt6 界面。

## 2026-09-10 · 用户补充调研文稿整合（01-开源项目调研 §6）
- 用户提供同类项目调研文稿（TradingAgents/ai-hedge-fund/MaverickMCP/Kronos/StockLLM/VTA + 技术选型建议），与本库比对整合：
  - **勘误**：文稿星标为旧快照（TradingAgents 69k / ai-hedge-fund 51.7k），以本库 09-10 API 实查 104.4k / 63.3k 为准。
  - **新增评估**：MaverickMCP（分层缓存思路记录，MCP 协议桌面 exe 不需要）、Kronos（K 线基础模型，torch 重依赖与打包冲突 → 五期后可选组件）、StockLLM（"检索历史相似形态+当时决策+实际结果"思路吸收进四期 RAG 反思设计）、VTA（学术方向确认我们"事实喂 LLM"路线正确，不引入）。
  - **取舍复核**：TA-Lib/pandas-ta 不采纳（打包/维护原因，自研指标已 8/8 单测）；跨厂商多模型路由暂不采纳（架构已预留）；NewsAPI 不采纳；内存热点缓存列二期按需项。
  - yfinance 2025-09 全线 401 事件补入踩坑台账"数据源"节作历史实证。
- 结论：文稿与本库既有调研和已实现架构高度一致，无推翻性冲突；仅吸收思路级增强点，选型不变。

## 2026-09-10 · 二期完成：PyQt6 图形界面（分析/K线图/决策记录）
- **内核适配**（为 UI 服务，CLI 兼容）：
  - graph.py：新增 `NODE_LABELS`（节点中文名）；`gather_facts/run_analysis` 支持 `progress_cb`（kind="data" 采集阶段文本 / kind="node" 节点完成通知）；progress_cb 存在时改用 `graph.stream(stream_mode="updates")` 逐节点汇报并增量合并 state；新增 `_delta_degraded`/`degraded_nodes` 降级节点检测（CLI 的 errors 字段从此真正有值）。
  - llm.py：`LLMRunner(mock=None)` 参数可显式覆盖（UI 的"离线演示"勾选不再依赖导入期环境变量）。
- **新增 app/ 界面层**（7 个文件）：
  - app/main.py：入口（`venv python -m app.main`），QApplication + setup_logging。
  - app/ui/main_window.py：三标签主窗口 + 免责声明状态栏；联动：分析完成→刷新决策记录+K线图自动载入该标的。
  - app/ui/analysis_tab.py：标的输入/离线MOCK勾选/开始按钮；**AnalysisWorker(QThread)** 后台跑管线（UI 不卡死）；左列 8 节点进度（⏳/✅/⚠️降级）+ 采集日志；右侧 QTextBrowser.setMarkdown 渲染报告；报告自动存 data/reports/ + "导出报告为…"对话框；失败红字提示不崩溃。
  - app/ui/chart_tab.py：pyqtgraph 蜡烛图（自绘 CandlestickItem，涨红跌绿）+ MA5/10/20/60 图例 + 布林带开关 + 成交量副图（颜色随涨跌）+ 整数索引→日期自定义轴；价格/成交量 X 轴联动；显示根数可调（60~1200，默认250）；_Fetch(QThread) 后台取数；缓存命中即时渲染（6002 根秒开）。
  - app/ui/history_tab.py：决策记录表（# / 时间 / 标的 / 数据截止 / 价格 / 动作 / 仓位% / 置信%），来源 SQLite decisions 表，手动刷新+分析完成后自动刷新。
- **冒烟验证**（scripts/smoke_gui.py，QT_QPA_PLATFORM=offscreen）：窗口构建✅ → mock 全链路经 UI 信号跑通（8 节点全 ✅、报告 1958 字、决策 #9 落库）✅ → K线自动联动加载（cache 6002 根，显示 250，最新收盘 1285.13）✅ → 决策记录自动刷新（9 条）✅。compileall app+core 零错误；指标单测回归 8/8。
- 修复一处：chart_tab 漏导入 pyqtSlot（NameError），补导入即过。
- 二期遗留小项（三期顺手做）：图表页实时报价角标、多标的对比、界面主题美化。下一步=三期（模拟盘+决策反思+回测）或等充值后先做真实 LLM 复验。

## 2026-09-10 · 三期完成：模拟盘 + 决策反思 + 复盘统计
- **模拟盘内核**（core/portfolio/paper.py，SQLite 四表：account/positions/trades/equity）：
  - 虚拟初始资金 100 万（STOCKAI_PAPER_CAPITAL 可调）；execute(state) 按最终决策自动成交：买入=按 position_pct 占总资产买入（A股 100 股整手、美股 4 位小数/加密 6 位碎股，现金不足自动缩减），卖出/回避=清仓并记盈亏，观望=不动；
  - 每次成交/估值刷新记净值快照（微秒时间戳，防同秒覆盖——首次实现用秒精度被同秒 4 笔成交压成 1 条，已修）；mark_to_market 拉现价更新持仓估值；reset() 清空重置。
- **决策反思**（core/memory/reflection.py）：
  - evaluate_pending()：决策满 10 个交易日后自动评估实际涨跌（用决策日收盘为基准、+10 交易日收盘对比，point-in-time 无前视偏差）；写 realized_return_pct + 反思文本（方向一致/相反/观望记录）+ ts_reflected；directional 判定=买入看涨/卖出看跌。
  - inject_context(ticker)：同标的最近 5 条已评估决策注入交易员与风控 prompt（graph.gather_facts 装入 state.history；trader_node 要求 reasoning 说明如何避免重蹈过往方向性错误）——**"自生知识库/自我学习"闭环在此落地**。
  - decision_stats()：总决策/已评估/方向正确率/平均实际收益/最佳最差。
  - decisions 表迁移：新增 ts_reflected 列（init() 内 PRAGMA 检查 + ALTER TABLE，兼容旧库）。
- **UI**：新增"模拟盘"标签（第 3 个）：账户汇总（现金/市值/总资产/总盈亏红绿着色）+ 持仓表 + 净值曲线（pyqtgraph，涨红跌绿）+ 成交记录表 + 决策胜率统计；_Refresh(QThread) 后台拉现价估值不卡 UI；分析完成自动刷新。分析页新增"决策自动入模拟盘"勾选（默认开）；分析前自动 evaluate_pending 并在日志区播报。CLI run_analysis.py 加 --paper。
- **冒烟**（scripts/smoke_phase3.py 全 PASS）：交易闭环（A股 100 股整手/美股碎股 313.48 股/清仓/观望不动/净值 4 条）→ 反思评估（构造 30 交易日前决策，实际 -0.64%，反思文本与注入上下文核对通过）→ 统计 → 模拟盘 UI 渲染（持仓 1 只/成交 3 条/总资产 1,001,487）。GUI 冒烟回归 4 PASS，compileall 零错误。
- 三次踩坑修复：①净值快照秒级主键同秒覆盖→微秒；②冒烟构造决策被 limit 截断→limit 放大；③pyqtgraph 轴标签误用 plotItem.labels dict→setLabel()。
- 三期状态：核心完成。遗留（四期做）：图表页实时角标、主题美化、（四期）chromadb RAG 学习库+相似历史形态检索。冒烟脚本会往 decisions 插测试决策（id#10-13），不影响真实使用。

## 2026-09-10 · 四期完成：chromadb 学习库 RAG + 相似历史形态检索
- **RAG 学习库**（core/memory/rag.py，chromadb PersistentClient 存 data/chroma/，collection=learning，cosine）：
  - **嵌入选型**（重要决策）：DeepSeek 无 embedding 端点、sentence-transformers 的 torch 与 exe 打包冲突 → **自研 HashEmbedder**：中文 char-bigram+单字+英文词 token，blake2b 带符号哈希到 512 维、L2 归一化；离线/零下载/确定性，语义精度弱于神经嵌入但对同类文档检索足够 MVP，后续可平滑升级 BGE。调 chromadb 一律显式传 embeddings=，不依赖其 EmbeddingFunction 协议（跨版本稳定），并关闭匿名遥测。
  - **四类文档源**：①报告 index_reports（reports/*.md 按 ## 小节切块，幂等 id=md5(kind|ref|idx)）；②新闻 index_news（SQLite news 表）；③反思 index_reflections（已评估决策，自我经验也是学习资源）；④index_analysis 每次分析后自动入库（结论卡片+当次新闻，失败不影响主流程）。
  - search(query, kind, n) → [{doc, meta, score}]，score=1-余弦距离。
- **相似历史形态检索**（core/memory/patterns.py，StockLLM 思路的确定性实现）：
  - 近 20 交易日 log-return 向量 vs 全历史滑窗（sliding_window_view 向量化，6002 根毫秒级）余弦相似 TOP5，附**其后 10 交易日真实表现**（涨跌/最大回撤/最大上涨）+ 聚合（均值/上涨占比）；
  - **无前视偏差纪律**：候选窗口必须满足其后向窗口数据已存在（j+window+forward ≤ N-1），即匹配用当时真实可得数据；实测 TOP1 相似 78.2%（2002 年片段）。
- **管线接入**：AnalysisState 新增 patterns；gather_facts 计算 → trader/risk prompt 注入"历史相似形态检索…注意：历史相似不保证未来重演"；报告新增"八、历史相似形态检索"表（原八/九顺延为九/十）；run_analysis 分析完成自动 index_analysis。
- **UI**：新增"学习库"标签（第 4 个）：语义检索（关键词+类型筛选+相关度表）、形态检索（标的→TOP5 相似段+聚合统计）、一键入库按钮、库状态条；_RagWorker/_PatWorker 后台线程不卡 UI。
- **冒烟**（scripts/smoke_phase4.py 全 PASS，一次通过）：RAG 检索排序正确（止损查询命中 resource 0.31）+ 报告入库 36 块 → 形态匹配 5 段（TOP1 2002-07 相似 78.2%，后10日 -3.02%）→ mock 管线 patterns 注入报告 + 结论自动入库检索命中 → 学习库 UI 检索 10 条/形态 5 段（库内 58 条）。GUI 回归 4 PASS，compileall 零错误。
- 已知非阻塞：东财 F10 源连不上（一期已知遗留，自动降级，二期起待补雪球/新浪源）。
- 四期状态：核心完成。遗留（五期做）：全量 onedir 打包 exe + 启动冒烟；可选增强：BGE 神经嵌入替换 HashEmbedder、图表页实时角标、主题美化。

## 2026-09-11 · 五期完成：PyInstaller 全量打包 exe 交付（337.9 MB onedir，自检 exit=0）
- **交付物**：`dist/StockAIPredictor/`（StockAIPredictor.exe 31.3MB + _internal/ + .env + 使用说明.txt + 空 data/），便携式整目录拷贝即用；windowed 无控制台模式。
- **打包资产**：packaging/stockai.spec（onedir，STOCKAI_PACK_CONSOLE 环境变量切控制台/windowed）、scripts/pack_exe.py（一键构建+拷贝交付物+体积报告）、packaging/使用说明.txt。
- **冻结适配**：①config.PROJECT_ROOT 感知 sys.frozen → 数据目录落 exe 旁边；②windowed 下 stdout 为 None，setup_logging 跳过 StreamHandler 只留文件日志；③app/main.py 入口 socket.setdefaulttimeout(30) 兜底（akshare 部分请求无超时）。
- **新增运行模式**：`--selftest`（offscreen 构窗口+mock 冷库全链路，结果写 data/selftest.log，退出码判定）、`--nettest`（逐源网络直连诊断，写 data/nettest.log，用户排障可用）。
- **排障三轮（详见踩坑台账）**：①冷库自检挂"采集行情"→ 实为 akshare file_fold 数据文件未打包（FileNotFoundError calendar.json），非网络问题——spec 改 collect_all('akshare')；②sina 快源退化到 tencent（10s）→ py_mini_racer 的 mini_racer.dll 未收集，collect_all('py_mini_racer') 后 sina 恢复 1.3s；③RAG 入库报 No module named 'chromadb.api.rust' → chromadb 1.5+ rust 扩展，collect_all('chromadb')（体积 272→338MB，拖入 onnxruntime，可接受）。另修：冷库 decisions 表初始化顺序（run_analysis 开头 decision_log.init()）、selftest K线断言需事件循环等待后台线程。
- **最终验收（windowed 终版，全新冷数据）**：--nettest 三源全 OK；--selftest exit=0：窗口5标签→sina 冷取6002行→新闻18条→8节点 mock→RAG 入库19条→决策记录/K线联动全 PASS，全程约 6 秒；正常双击启动 GUI 进程存活。交付前清空 data/ 测试数据。
- **体积演进**：228.2（缺数据文件）→234.0（+akshare数据）→272.2（+mini_racer）→337.9MB（+chromadb rust/onnxruntime）。后续如需瘦身：onnxruntime 仅 chromadb 默认 EF 用（我们自嵌入不用），验证可排除后可减约 60MB。
- **五期状态：全部五期完成**。唯一外部前置：DeepSeek 充值后做真实大模型复验（mock 已证明工程链路完好）。

## 2026-09-11 · 沉淀打包技能 pyinstaller-windows-pack
- 路径：`.trae/skills/pyinstaller-windows-pack/SKILL.md`（工作区级技能，随项目走）。
- 内容：onedir 标准流程五步、冻结适配三代码段（sys.frozen 路径/stdout None/socket 超时）、spec 模板（collect_all akshare/py_mini_racer/chromadb + pyqtgraph 数据）、缺失依赖速查表（台账10-12 三坑）、--selftest/--nettest 自检模式要点、PowerShell Start-Process 退出码验收、纪律红线（冷库强制验收/证据导向/体积参考/Python3.14 无需降级）。下次打包或换机器直接套用。

## 2026-09-11 · 六期增补：本地离线 AI 引擎 + 蒸馏自学习闭环
- **背景**：DeepSeek 账户余额仍为 0（官方 /user/balance 实测 topped_up_balance=0，疑似充到网页版账号），用户要求先搭完全离线可用的本地 AI，并能上网学习/蒸馏/强化。
- **环境**：winget 装 Ollama 0.34.0；拉取 qwen2.5:7b-instruct-q4_K_M（4.7GB，带代理重启 daemon 后约 25 分钟，坑15）。
- **新增文件**：core/llm_local.py（Ollama 检测/拉起/拉取流式进度/OpenAI 兼容，回环绕代理）、core/training/{__init__,distill}.py（SFT+偏好双轨语料/LLaMA-Factory 导出）、app/ui/local_model_dialog.py（状态/7B+3B 选择/进度条/安装指引）、scripts/smoke_local_llm.py（假 Ollama 端点 12 项冒烟）、scripts/export_distill.py。
- **改造**：core/llm.py 三后端路由 deepseek/local/auto（402/断连 auto 切本地，token 记账本地成本0，成功即写 SFT 语料 teacher/student）；schemas.py 全字段校验前语义归一化（坑16）；reflection.py 评估到期决策自动写偏好标签；analysis_tab.py 引擎下拉+本地预检引导；knowledge_tab.py 语料统计+导出按钮；run_analysis.py --backend/--local-model。
- **真机验收**：SH600519 backend=local 三次跑通——首次 trader 因 entry=null 降级（修）、二次 8/8、三次 8/8 且风险段干净；8 节点 26~34 秒/约 1 万 tokens/0 元；SFT student 语料自动积累（17+条）。
- **冒烟**：假端点 12/12（本地路由、auto 真实 402→切本地、成本 0、语料落盘、偏好打标、导出往返）；schema 脏值归一化断言通过；指标单测 8/8 回归；GUI offscreen 五标签+引擎三选项+模型对话框构建通过。
- **遗留/后续**：① 用户需确认 DeepSeek 充值账号（platform.deepseek.com 与聊天版账号体系不同）；② UI"带代理重启 Ollama"自动化待做（现在卡 manifest 时需手动，坑15）；③ DPO 配对脚本（教师对 bad 样本重生成 chosen）待云端可用后补；④ 待重新打包 exe（无新增原生依赖，spec 预计不用改）。
- **打包收尾（当日完成）**：spec hiddenimports 显式补 core.llm_local/core.training.distill；新增 exe `--localping` 自检（Ollama 状态+真实推理，落 data/localping.log）。windowed 重打 103s、体积仍 337.9MB（无新增原生依赖）；**双验收全过：--selftest exit=0（冷库冷取6002行+8节点+RAG+联动）、--localping exit=0（冻结 exe 真实 7B 推理 4.3s/0 元）**。交付目录恢复 .env+新使用说明、清空验收 data、删 _bak。打包技能补 localping 三件套。

## 2026-09-11 · 七期增补：教学资源蒸馏训练（教学资源→Q&A→QLoRA→导入）
- **背景**：用户要求训练离线 AI 模型，蒸馏网上全球经济金融教学资源。六期已有蒸馏闭环（程序自身推理语料），本期扩展数据来源为外部教学资源。
- **新增模块**：core/training/resources.py（14 个开放许可源注册+获取+解析，复用 config foreign/domestic 网络上下文，lazy import pdfplumber/trafilatura 不进打包）、curriculum.py（教材切块1800字→本地7B教师生成Q&A→去重→质检→record_sft source=curriculum）、ollama_import.py（venv_train 编排 train/merge/ollama create/写.env）；app/ui/training_dialog.py（四页对话框，后台线程）；scripts/{bootstrap_venv_train.ps1,fetch_resources,train_offline}；training_configs/{qwen2_5_qlora_curriculum.yaml,merge_lora,dataset_info,Modelfile.stockai,LICENSE_MANIFEST.md}。
- **改造**：core/llm.py chat_json 加 raw 参数（raw=True 返回原始 content 不解析不记 SFT，供教师批量生成）；distill.py export_llamafactory 加 sources 过滤（curriculum_sft.json）；local_model_dialog 加"训练离线模型"入口；spec hiddenimports 补 4 模块。
- **关键决策**：训练独立 venv_train（Python 3.11，不污染主 venv/打包）；教师用本地7B自蒸馏（DeepSeek 余额0且要离线）；QLoRA 4bit 保守配置（cutoff_len512/lora_rank8/gradient_checkpointing/paged_adamw_8bit）适配8GB显存；只收开放许可资源（CC-BY-SA/官方投教）。
- **验收（蒸馏链路全通）**：① 取资源 csrc_risk_edu 26775字符+wiki_stock_zh 9599+wiki_stock_en 43050+wiki_ta_en 57580 共4文件169445字符（investopedia 403 Cloudflare 弃用换 wiki，pbc 404 弃用，见坑17）；② 蒸馏 csrc 18块→69对Q&A（source=curriculum），抽查证监会职能问答准确；③ 导出 curriculum_sft.json 69行；④ selftest exit=0 回归（主流程未破坏）；⑤ UI 四页对话框 offscreen 构建通过（坑18 Qt.ItemDataRole.UserRole）。
- **训练阶段前置**：本机未装 Python 3.11（仅有3.14，坑19），venv_train 建不起来；bootstrap_venv_train.ps1 就绪，用户装 3.11 后跑即可（torch cu121+llamafactory）。训练需下载 Qwen2.5-7B fp16 ~15GB（HF镜像），QLoRA 1 epoch，8GB 显存保守配置见坑20。
- **遗留**：用户装 Python 3.11→跑 bootstrap→train_offline all（distill→export→train→merge→import）完成真实训练；investopedia 反爬已用 Wikipedia 替代。

## 2026-09-11 · 七期完成：QLoRA 训练→合并→Ollama 导入全程跑通
- **环境搭建**：Python 3.11.9 静默安装→venv_train 创建→torch 2.11.0+cu128（RTX 5060 Blackwell 需 CUDA 12.8+）+torchvision 0.26.0+cu128+torchaudio 2.11.0+cu128+LLaMA-Factory 0.9.5 安装完成。CUDA 验证 torch.cuda.is_available()=True，GPU=NVIDIA GeForce RTX 5060 Laptop GPU。
- **模型下载**：Qwen2.5-3B-Instruct（~6GB fp16）从 hf-mirror.com 下载。huggingface_hub 0.36.2 直传 HF_ENDPOINT 遇 FileMetadataError（镜像 HEAD 响应头不满足 hub 校验，坑21），改用 hf_transfer 多连接并行下载成功（8 连接 10MB chunks，均速 ~2.8MB/s，约 35 分钟）。下载到 D:\StockAIPredictor\models\Qwen2.5-3B-Instruct\（9 文件：config/tokenizer×5 + safetensors×2 + index）。
- **训练配置修复**：LLaMA-Factory 0.9.5 的 `--dataset_dir` 不能作为 CLI 参数（HfArgumentParser 报 unused_keys），改为写入 YAML 配置的 `dataset_dir` 字段（坑23）。model_name_or_path 从 `Qwen/Qwen2.5-3B-Instruct`（HF Hub ID）改为本地绝对路径 `D:/StockAIPredictor/models/Qwen2.5-3B-Instruct`。
- **QLoRA 4bit 训练**：Qwen2.5-3B-Instruct 基座，351 对 curriculum Q&A（5% 验证=333 train + 18 eval），LoRA rank=8/alpha=16/dropout=0.05，4bit nf4 双重量化，gradient_checkpointing，paged_adamw_8bit，cutoff_len=512，batch=1×grad_accum=8，1 epoch=42 steps。
  - **结果**：train_loss 2.357→1.530（收敛良好），eval_loss=1.882，train_runtime=107.77s（1分48秒），可训练参数 14,966,784/3,100,905,472=0.48%。
  - loss 曲线：2.36→1.84→1.61→1.84→1.46→1.55→1.56→1.53，呈下降趋势。
- **LoRA 合并**：llamafactory-cli export merge_lora.yaml → bfloat16 合并 4 分片（1874+1868+1868+276=5886MB），输出 D:\StockAIPredictor\data\training\merged\，LLaMA-Factory 自动生成 Ollama Modelfile。
- **Ollama 导入**：ollama create stockai-qwen-curriculum（从 merged 目录，Modelfile FROM . + ChatML 模板 + 金融分析 SYSTEM 提示），6.2GB，导入成功。
- **验证测试**：API 调用 `什么是市盈率？` → "市盈率是衡量股票价格相对于每股收益的比率，用于评估股票的估值水平。计算公式为：市盈率=股票价格/每股收益。"（35 tokens, 0.9s, 39 tok/s）——蒸馏的教学知识已入模型。
- **设为本地学生模型**：.env `STOCKAI_LOCAL_MODEL=stockai-qwen-curriculum`。Ollama 现有两模型：stockai-qwen-curriculum:latest（6.2GB 蒸馏版）+ qwen2.5:7b-instruct-q4_K_M（4.7GB 原始版）。
- **临时脚本**：scripts/download_model_hftransfer.py（hf_transfer 多连接下载，可复用于其他模型）。
- **七期状态：全部完成**。离线 AI 模型已训练完成并导入 Ollama，可完全离线推理。后续优化建议：增大数据集、多 epoch 训练、DPO 偏好对齐、7B 基座（需更大显存）。

## 2026-09-11 · 八期：策略回测引擎（第二阶段缺口补齐，零新增依赖）
- **背景**：用户给出长清单功能扩展建议；先把建议与现有代码逐条对照——多智能体辩论/RAG/分层缓存/模拟盘/决策反思/风险留痕/加密/离线AI均已有，清单中"你目前是单Agent"系误判（一期即8节点）。按用户自定路线图与"不要同时铺开"原则，确定第八期只做**策略回测引擎**（第二阶段唯一缺口，也是后续多因子/MoE/FinBERT 能否验证的标尺）。计划书 `.trae/documents/第八期_策略回测引擎_plan.md` 经用户批准后执行。
- **新增** core/quant/backtest.py：纯 pandas 事件驱动日线回测，零新依赖。
  - **防作弊三纪律**：信号 t 日收盘用截至 t 数据生成 → **t+1 开盘成交**（末根不挂单，专项单测锁死）；指标预热 NaN 窗不出信号；真实成本（CN 佣金万2.5最低5元+卖出印花税千0.5+过户费十万1，US 万3，CRYPTO 千1，滑点 bps 可配）。
  - 单标的只做多 all-in/out，CN 整手100/US 4位小数/CRYPTO 6位；仓位比例可配。
  - 策略注册表：rsi_reversion（RSI周期/买阈30/卖阈70）、ma_cross（5/20 金叉死叉）、macd_cross（12/26/9）、buy_hold（首日开盘满仓基准）；STRATEGY_META 登记 UI 元信息，加策略只加一个函数。
  - 指标：总收益/年化（252日）/基准收益/超额/最大回撤/夏普(rf0)/胜率/盈亏比/平均持仓天数/总费用/期末权益；胜率按完整买-卖回合。
- **新增** scripts/run_backtest.py（CLI，参数走 argparse；结果 JSON 落 data/backtests/ 留痕）、app/ui/backtest_tab.py（第6个 GUI 标签"回测"：标的+策略+参数+资金/仓位/起止日期，QThread 后台，指标表+pyqtgraph 权益曲线 策略vs基准虚线+成交流水表，结果同样落盘）、tests/test_backtest.py（11 个零依赖 assert 单测，延续 test_indicators 风格）、scripts/smoke_backtest_gui.py（offscreen 冒烟）。
- **改造** main_window.py 注册第6标签（现 分析/K线图/模拟盘/回测/学习库/决策记录）；app/main.py selftest 标签数改动态 count()（原文案硬编码5）；core/quant/__init__.py 导出 run_backtest/BacktestConfig/STRATEGY_META。
- **真实验证**：SH600519 全历史 6002 根 RSI 策略 42 笔成交、胜率76.19%、盈亏比1.12、总收益196% vs 买入持有32937%（前复权25年涨幅，正确，说明均值回归长期跑输茅台牛股）；2020起 ma_cross 99笔/胜率32.65%/盈亏比2.2；AAPL 买入持有 +46.66% vs 基准+47.23%（差-0.57%=费用+滑点）；2023起 RSI 10笔/胜率100%/总收益40.68%/回撤-25.9%。所有成交日=信号日次一交易日。
- **回归**：compileall 零错误；test_indicators 8/8、test_backtest 11/11；GUI offscreen 6标签+策略切换+渲染全 PASS；--selftest exit=0（冷库6003行+8节点 mock+RAG+联动，文案显示"6 标签"）。
- **打包**：backtest_tab 被 main_window 静态导入，PyInstaller 自动跟踪，spec 无需改（下次重打 exe 生效，本期未重打）。
- **明确未做（后续期）**：AI信号历史回放、组合级回测、参数寻优、Walk-forward、港股/ETF、研报公告、PDF 导出、暗色主题、自选股盯盘、MoE/FinBERT/Kronos。
- **八期状态：完成**。

## 2026-09-11 · 九期完成：自选股 + 一句话盯盘 + 桌面通知
- **背景**：八期后从用户长清单选 option B（自选股盯盘通知）——二期规划的自选股面板被跳过（现仅6标签），程序仍"被动分析"。计划书 `.trae/documents/第九期_自选股盯盘通知_plan.md` 经用户批准后执行。
- **关键决策**：盯盘条件用**规则模板引擎解析，不调 LLM**。理由：①DeepSeek 余额待充值、高频盯盘调 LLM 成本/延迟不可接受；②规则确定性可复现零成本；③符合"数字程序算好、LLM 只解读"纪律。仅支持固定语法模板，不支持任意自然语言（解析失败给语法提示）。
- **新增 core/watchlist/**（零新依赖，沿用 get_conn 持久化）：
  - store.py：watchlist 表（ticker/market/condition/note/enabled/added_at/updated_at/last_trigger_at，UNIQUE(ticker)）；CRUD add/list/list_enabled/get/update/set_enabled/remove/set_last_trigger。条件原文存库，**解析结果不落库**（每次扫描重解析，规则升级即生效无脏数据）。
  - conditions.py：normalize（全角→半角/中文数字→阿拉伯/去空格）+ 六类正则 fullmatch 解析（价格MA/价格常量/涨跌幅/RSI/MACD/成交量）→ Condition{kind,field,op,value,direction,raw,error}；evaluate 穿透判断（跌破=close<MA 且 prev≥MA，防反复弹）+ 状态比较；任何异常吞掉返回 ❌无效/—无数据，绝不外抛。
  - engine.py：scan_all() 批量 get_realtime+get_daily+latest_snapshot+逐条评估+错误隔离；RETRIGGER_MIN（STOCKAI_RETRIGGER_MIN 默认60分钟）防同一条件短时反复弹。
- **改造**：core/config.py 新增 is_market_open/is_us_dst/refresh_interval（A股9:30-15:00、美股夏令时21:30-次日04:00冬令时顺延1h、加密24h、凌晨段按前一日工作日判断；纯 date 公式不引 zoneinfo 保打包体积）；app/ui/main_window.py 注册第7标签"自选股"（放首位）+ QSystemTrayIcon 托盘（isSystemTrayAvailable 不可用时跳过）+ watchlist.analyze_requested/triggered 联动；app/main.py selftest 增 watchlist 冒烟（TESTWATCH 无效标的验证错误隔离）。
- **UI**：watchlist_tab.py 控制条（标的/市场自动识别/条件/备注/添加/测试条件）+ 看板表（标的|市场|现价|涨跌%|条件|状态|距条件|更新时间，涨红跌绿+状态色）+ 右键菜单（加入分析/编辑条件/停用/删除）+ 双击加入分析 + 开始盯盘 CheckBox（QTimer 按 refresh_interval 动态调频）。
- **验证**：tests/test_watchlist.py **32/32** 过（解析/归一化/评估/交易时段/CRUD）；回归 test_indicators 8/8、test_backtest 11/11（共51全过）；compileall 零错误；--selftest exit=0（7标签+mock全管线观望0%+决策记录联动+K线联动+watchlist错误隔离1行不抛）。
- **踩坑**：①watchlist 表需在 WatchlistTab.__init__ 实例化时 store.init()（原计划在 selftest 后段才 init，但 _load_list 先查表→ OperationalError no such table）；改在 UI 实例化即建表，自给自足。②TESTWATCH 走 yfinance 报 'currentTradingPeriod' 错，被 engine 错误隔离捕获，验证错误隔离纪律。
- **明确未做（后续期）**：真实多标的(AAPL/BTC-USD/0700.HK/SH510300)人工盯盘验收、重打 exe（下次重打生效）、分钟级评估（现基于日线 latest_snapshot+实时价）、LLM 兜底解析（余额充值后可选）、组合级盯盘聚合通知。
- **九期状态：完成**。

## 2026-09-11 · 十期完成：港股 / A股ETF 市场覆盖（零新依赖）
- **背景**：九期后推进用户长清单 option C。先做联网实测再设计（非假设）：①港股 0700.HK yfinance 日线492根/实时428.4（腾讯控股）已通，缺市场规则；②A股ETF 实测新浪 stock_zh_a_daily **不支持**（JSONDecodeError）、腾讯 stock_zh_a_hist_tx **支持**（sh510300 3476行/sz159915 3584行）、新浪实时 hq.sinajs.cn 支持（沪深300ETF华泰柏瑞 4.579）、东财 fund_etf_hist_em 受443断连限制；③美股ETF（SPY）market_of=US+yfinance 零改动天然支持。计划书 `.trae/documents/第十期_港股ETF覆盖_plan.md` 经用户批准后执行，全部验证自动化（用户明确跳过人工环节）。
- **改造**（9 文件，零新依赖）：
  - core/data/service.py：market_of 在通用"."分支**之前**加 `\d{4,5}\.HK → HK`；新增 is_cn_etf（沪 51x/56x/58x、深 159 段正则）与 security_type；get_daily 分支含 HK；get_fundamentals 对 CN ETF 直接返回说明 dict（不调 F10）。
  - core/data/akshare_source.py：三源拆为 _daily_sina/_daily_tencent/_daily_eastmoney（etf 开关走 fund_etf_hist_em）；**ETF 降级顺序=腾讯→东财ETF**（跳过不支持的新浪），股票顺序不变 新浪→腾讯→东财；腾讯多出 turnover 列由 _norm 六列裁剪天然兼容。
  - core/config.py：is_market_open 加 HK（周一~五 9:30-12:00/13:00-16:00，港股本地时间=北京时间，午休1.5h）。
  - core/quant/backtest.py：default_fee(market, ticker=None) 加 HK（佣金万3/min0、卖出印花税**0.0013**、双边杂费万0.3计入 transfer_fee、滑点8bps）与 CN ETF（佣金万2.5/min5、**免印花税免过户费**）；resolve_fee 用 self.ticker；_lot_shares/_size_position 整手集合改 {CN,HK}（港股真实每手因股而异 100/200/500，yfinance 无 lot size，MVP 统一100近似并注释）。
  - core/portfolio/paper.py：_lot_shares HK 整手100（模拟盘不计费，ETF 免税天然一致）。
  - core/agents：nodes 风控 cap 加 HK:25% 与 **CN ETF:40%**（分散化产品额度高于个股）；prompts MARKET_NAME 加港股、SYS_RISK 文案补 ETF/港股、header() 加"·场内基金ETF"标注；graph.py state 加 security 字段，采集日志显示"CN（场内ETF）"。
  - UI：分析页/自选股页 placeholder 加 SH510300 与 0700.HK 示例（其余市场识别/费率自动生效）；CLI 已传 ticker 无需改。
- **新增** tests/test_markets.py 9 用例（HK 识别含00700.HK五位、ETF 号段含边界、security_type、港股时段含午休/周六、HK/ETF/股票费率、整手、HK 回测 buy_hold 股数100倍数）。
- **全自动验证（全绿）**：compileall 零错误；单测 60 个全过（indicators 8 + backtest 11 + watchlist 32 + markets 9）；真实门面 get_daily SH510300=3476行(tencent)/0700.HK=492行、实时与 ETF 基本面 note 断言通过；STOCKAI_MOCK=1 双标的 8 节点跑通（日志正确显示 HK 与 CN（场内ETF），SH510300 新闻18条）；真实回测 0700.HK buy_hold 买入2700股(整手) 费用320.34=佣金+徵费无印花税、总收益18.56%；SH510300 RSI 策略13回合/胜率61.54%，卖出109万费用273.63（纯万2.5佣金，免印花税验证：若股票应收~820）；--selftest exit=0（7标签+全链路+watchlist隔离）。
- **明确不做**：akshare 港股专用源（yfinance 已通）、港股真实每手 lot 表（统一100近似）、债券/期货/期权/REITs、美股盘前盘后、重打 exe（下次生效）。
- **十期状态：完成**。

## 2026-09-11 · 十一期完成：AI 信号历史回放（AI 决策质量标尺）
- **背景**：八期规则回测验证的是 RSI/均线等规则策略；本期验证 **AI 多智能体管线本身**的决策质量——在历史每个时点只凭当时数据跑 8 节点，再用 t+5/10/20 日真实走势检验方向命中率。与三期 reflection 的区别：reflection 被动评估当下真实分析（样本少、混 decisions 表），本期是主动批量历史回放、独立成表、不污染真实统计；也是后续 DPO 偏好对齐的样本来源。计划书 `.trae/documents/第十一期_AI信号历史回放_plan.md` 经用户批准执行。
- **设计纪律（含历史回放系统经验）**：①选点时间基准只来自 bars 实际交易日索引（不用系统时间/不伪造，点不足直接 ValueError）；②回放信号入独立表 ai_replay_signals（schema_version=1），**不写 decisions、不入 RAG、不进反思注入**；③as_of 时点严格切片：bars 截断 index<=as_of、quote 用末根 K 线构造不取实时、news 按 published_at<=asof 过滤（日期无法解析保守丢弃防前视）、反思上下文置空、patterns 用切片 bars；④基本面免费源无历史值，保留当前快照并打 _as_of_note 局限标注。
- **改造 core/agents/graph.py**：gather_facts 加 as_of 参数（回放模式：bars 截断<60根报错/quote 构造/新闻过滤/history=[]/funda 标注）；抽出 execute_state() 复用图执行；run_analysis 加 as_of/persist 参数（as_of 非空时不 save_decision、不 index_analysis）。
- **新增 core/replay/**：store.py（ai_replay_signals 表：batch_id/ticker/market/engine/asof/action/position/ret_5/10/20/hit_5/10/20/evaluated/error，UNIQUE(batch_id,ticker,asof)，save_signal/list_batch/batches/update_evaluation）；engine.py（pick_dates warmup120+前向20根保护；run_replay 逐点 run_analysis(persist=False) 单点失败记 error 行不中断；evaluate_batch searchsorted 定位+多空方向判定（买入/加仓=多，卖出/回避=空，观望不计胜率）；batch_stats 各 horizon 胜率/均收/多空分列/信号分布）。
- **入口**：scripts/run_replay.py（--start/--end/--step/--engine mock|deepseek|local，默认 mock 零成本，明细表+汇总表，落 data/replay/ JSON）；app/ui/replay_tab.py 第8标签"信号回放"（参数条+进度+汇总卡片+9列明细表+历史批次下拉+局限免责）；main_window 注册；selftest 增回放冒烟（固定批次 SELFTEST 幂等覆盖，2 点评估+decisions 计数不污染断言）。
- **验证（全绿）**：tests/test_replay.py 8 用例（选点/切片/隔离/方向/统计/错误行）；五套单测共 **68 全过**（replay8+watchlist32+markets9+backtest11+indicators8）；compileall 零错误；真实数据回放 SH600519 9点/SH510300 43点/0700.HK 6点全部成功评估并落 JSON，收益计算正确（如 SH510300 2024-08-29 后20日+23.68%）；--selftest exit=0（8 标签+回放冒烟 decisions 不污染）。
- **重要发现/局限**：mock 桩买入条件是 facts 文本含"低于30"字样，而技术 prompt 实际写"超卖"，故 mock 回放几乎全观望（茅台9/沪深300ETF43/腾讯6 全中性）——方向评估逻辑由单测构造数据验证正确；**真实多空信号回放须待 DeepSeek 充值后 `--engine deepseek`**（一条命令即可，费用启动前提示）。免费源无完整历史新闻/F10，回放主要验证"技术面→AI决策"链路，UI/CLI 均标注。
- **明确不做**：真实 LLM 批量回放（引擎已预留）、历史新闻/基本面回填、参数寻优/Walk-forward、接模拟盘、重打 exe。
- **十一期状态：完成**。

## 2026-09-11 · 十二期完成：策略参数寻优（网格+绩效平原+Walk-forward）
- **背景**：八期回测只能跑单参数组合；本期自动网格搜索并回答"最优参数是真稳健还是过拟合尖峰"。按经验 2013803 落地三道防过拟合关：目标排名、平原评估、IS/OOS 跨期验证。plan `.trae/documents/第十二期_参数寻优_plan.md`（用户预授权十二/十三连做，未设批准门）。
- **新增 core/optimize/grid.py**：expand_grid 笛卡尔积（空网格/超 500 组合报错）；run_grid 每组复用 run_backtest（同 df 纯计算），0 交易组合标记排末尾；OBJECTIVES=sharpe(默认)/return/calmar；**assess_plateau 用指标语义容忍带的邻居达标率**（sharpe±0.1/收益±5pct/calmar±0.2，robust=邻居同质比例，≥0.7 平原/<0.4 尖峰）；**walk_forward** 按行数 6:4 切 IS/OOS，IS 全网格选参→OOS 看该参数在全网格的排名分位（≥0.6 跨期稳健/<0.4 失效警示）；DEFAULT_GRIDS 内置三策略候选；optimize 总入口。
- **入口**：scripts/run_optimize.py（参数候选逗号传入，如 --fast 3,5,10 --slow 20,30,60；不传用默认网格；落 data/optimize/ JSON）；app/ui/optimize_tab.py 第9标签（动态参数网格行/目标/IS比例/最优+平原+WF 三色结论卡/top15表）；selftest 增寻优冒烟（1组合 IS/OOS）。
- **测试**：tests/test_optimize.py 11 用例（笛卡尔积/上限/排名/0交易/三目标/平原vs尖峰纯函数/无邻居/WF结构/坏split/端到端/默认网格）。**关键踩坑**：平原 robust 初版用"全网格极差归一"，但平原场景极差本身就是邻居小差异，导致 0.95/0.97/1.00 被判尖峰（robust=0.2）；改为指标语义固定容忍带后正确。
- **验证（全绿）**：compileall 零错；**79 单测全过**（optimize11+replay8+watchlist32+markets9+backtest11+indicators8）；真实寻优两案例双路径均触发——SH510300 ma_cross 全区间最优(10,60)夏普0.453 平原robust=1.0，但 IS 0.735→OOS -0.242、**OOS分位0.25 跨期失效警示**；SH600519 rsi_reversion(14,30,80) **OOS分位0.86 跨期稳健**（OOS年化9.76%）；selftest exit=0（9标签）。
- **不做**：遗传/贝叶斯优化、与十三期组合回测文件级联动（参数手动复制）、重打 exe。
- **十二期状态：完成**。

## 2026-09-11 · 十三期收尾：AI 后端切通义 + 全量回归 + 重打 exe
- **背景**：用户 Trae 免费积分在编码中途烧光，要求"充分学习后把项目收尾做到极致、不出 bug"。经探查项目已做到十三期、代码成熟，并非没写完，而是 Trae 积分耗尽中断。
- **知识库+对标学习**：通读 docs/kb 全部（00总索引/03踩坑台账27坑/操作日志）+ 十三期 multi_backtest.py / test_portfolio.py；联网对标 TradingAgents(TauricResearch)、ai-hedge-fund、Vanguard 再平衡研究。结论：本项目"数字程序算好、LLM 只解读、Risk 确定性兜底"方向正确；业界多出的阈值再平衡(drift>5%)、风险平价、蒙特卡洛属 v2 增强，v1 不引入以免新 bug。
- **关键纠错（测试为何曾是 0）**：测试文件用自带 `_run_all()` + `if __name__=="__main__"`，不是 unittest.TestCase，`unittest discover` 永远发现 0 个。必须直接 `python tests\test_xxx.py` 运行。
- **AI 后端切换（核心收尾）**：原配 DeepSeek 余额 0 返回 402，真实云端 LLM 从未跑通。备份 `.env`→`.env.bak_deepseek`；新 `.env` 把云端指向**通义千问百炼 OpenAI 兼容端点**（base `https://dashscope.aliyuncs.com/compatible-mode/v1`，模型 qwen-max/qwen-flash，免费额度已开"用完即停"）。config.estimate_cost 加"非 deepseek 模型返回 0.0"避免按 DeepSeek 单价报假账。llm.py 逻辑零改动（后端名 deepseek 只是 OpenAI 兼容分支标签，base_url 改指即切）。
- **真实验证**：单节点 chat_json 返回结构化 JSON（stance/要点/风险齐全）；`run_analysis SH600519` 完整 8 节点**真实**跑通，46 秒出"观望 0% 仓位"，报告落盘 data/reports/，累计 10174 tokens、cost=¥0.0。
- **全量回归**：7 个测试文件 **88 用例全过**（backtest11+indicators8+markets9+optimize11+portfolio9+replay8+watchlist32），0 失败 0 错误。
- **重打 exe**：scripts/pack_exe.py 重打 onedir **338.0 MB**（含九~十三期新功能 + 通义 .env）；`--selftest` 冷库 **7/7 PASS**（窗口构建/全计算观望0%/决策联动/K线联动/watchlist错误隔离/信号回放/参数寻优）。
- **坑**：①PyInstaller 把 INFO 写 stderr，PowerShell `Tee` 后误报 NativeCommandError(exit1)，实为构建成功，看末尾"Build complete"判定。②akshare F10 偶发 RemoteDisconnected，基本面节点空，属数据源抖动非 bug。③国内服务(通义)必须 trust_env=False 绕 Clash 代理。
- **十三期收尾状态：完成。项目可双击即用，真实云端 AI（通义 qwen-max）首次跑通。**
- **补漏（2026-09-12 凌晨，用户实测双击 exe 打不开）**：弹窗 `NameError: name 'QApplication' is not defined`（main.py:221）。**根因**：`main()` 函数体用了 `QApplication(sys.argv)` 却没 import；`_selftest()` 分支在第 32 行自己 import 了，正常 GUI 分支漏写。**关键教训：--selftest 走的是 `_selftest()` 分支，根本不经过 `main()` 的 GUI 构建路径，所以 7/7 PASS 也会漏报这个启动 bug**。修法：`main()` 内 `setup_logging()` 后补 `from PyQt6.QtWidgets import QApplication`。**验收标准升级**：光 selftest 不够，必须 offscreen 启动 exe 看进程是否存活（进事件循环）才算真启动成功。重打后 exe 进程存活 + selftest 7/7 双确认。
- **功能增强（2026-09-12，豆包接手续做三期）**：
  1. 多因子体系 `core/quant/factors.py`：六因子（动量/趋势/RSI位置/低波/量价/估值PE）纯函数，-1~+1 标准化、合成 0~100，缺失因子自动跳过加权；接入 gather_facts 喂技术分析师+风控。单测 `tests/test_factors.py` 9/9。
  2. 市场状态路由 `core/quant/market_regime.py`：MA200/MA50 结构判牛/熊/震荡，熊市仓位系数×0.5、震荡×0.75、牛市×1.0，风控单标的 cap 动态下调。单测 `tests/test_regime.py` 7/7。
  3. 暗色金融终端主题 `app/ui/theme.py`：全局深色 QSS，main() 挂 `apply_dark_theme(app)`。
  4. `core/report.py` 报告新增"多因子与市场状态"一节；分析页 Markdown 导出按钮 Trae 已具备。
  5. 评估结论：FinBERT/Kronos 需下载数 GB 模型+torch/transformers，与"双击即用 exe"冲突且极易打包失败，不强行塞入；现有 qwen 新闻情绪节点 + 量价情绪因子已覆盖该诉求。
  - 全量回归 9/9，重打 exe 338MB，offscreen 启动进程存活验证通过。
- **功能增强第二轮（2026-09-12，ML+宏观）**：
  6. ML 集成信号 `core/quant/ml_signal.py`：装 scikit-learn 1.9.1（cp314 scipy wheel，Python3.14 兼容），随机森林从11个技术特征学未来5日方向，输出上涨概率+top因子贡献（等价SHAP全局解释），class_weight=balanced+min_samples_leaf防过拟合，数据不足/单类别优雅降级。单测 tests/test_ml_signal.py 8/8。接入技术分析师+报告。
  7. 宏观市场概览 `core/data/market_overview.py` + `app/ui/overview_tab.py`：新浪 s_ 指数实时（上证/深成/创业板/沪深300/中证500/上证50），红涨绿跌，后台QThread刷新。设为主窗口首页标签。
  - 全量回归 10/10，重打 exe 426MB（含sklearn/scipy），offscreen 启动进程存活验证通过。
  - **未做（如实记录）**：Kronos/FinBERT 需 torch+大模型下载，主venv Python3.14 无torch，打包脆弱，留作实验版不进主exe；Tushare/Tavily/Longbridge 需用户提供新 key。
- **功能增强第三轮（2026-09-12，本地大模型落地）**：
  8. A/B融合 `core/quant/ensemble.py`：多因子(0.55)+ML上涨概率(0.45)+市场状态轻压分 → 统一"程序量化综合分"0~100+verdict，作风控确定性参考。单测7。
  9. FinBERT中文金融情感 `core/ml/finbert.py`：主venv成功装 torch2.14.0+cpu（**Python3.14兼容**）+transformers5.17.0。模型 `yiyanghkust/finbert-tone-chinese`（注意：finbert-zh 不存在，list_models确认正确名），hf-mirror下载。验证：利好0.999/利空-0.997/一多一空中性0.008。接入情绪节点+报告。
  10. Kronos K线预测 `core/ml/kronos_forecast.py`：克隆官方 GitHub 到 external/kronos（pip无包），sys.path导入 `model.Kronos/KronosTokenizer/KronosPredictor`，from_pretrained NeoQuasar/Kronos-small（max_context512，需einops）。取近400日OHLC预测未来5日方向。CPU推理毫秒级。接入技术节点+报告。
  - spec改：excludes去掉torch、collect_all(torch+transformers)、pathex加external/kronos。
  - 全量回归 11/11。
- **功能增强第四轮（2026-09-13，数据源）**：
  11. Tushare `core/data/tushare_source.py`：token已入.env。**实测新号积分不足**，daily/daily_basic均"无访问权限"；代码正确，积分够了自动生效，当前在日线降级链末端静默兜底。
  12. Tavily `core/data/tavily_news.py`：dev key已入.env，海外服务走代理。实测搜茅台返回实时新闻5条。已并入 service.get_news 的A股新闻。
  - 全量回归 11/11。
  - **待办**：Longbridge用户自行开户；多LLM辩论需第二个模型key（用户通义额度紧张）。
- **功能增强第五轮（2026-09-13，多LLM辩论 key 就位）**：
  13. Cohere key：bu自动化在 dashboard.cohere.com/api-keys 创建 trial key（名stockai），存入.env。
      **关键坑**：command-r、command-a-03-2024 均已下线/无权限；实测唯一可用模型=`command-r-08-2024`。trial限额仅20次调用（已用3），很紧。
  14. OpenAI key：bu在 platform.openai.com/api-keys 创建（名stockai，永不过期），存入.env。**但账号未充值（页面Add credits），GPT调用会402，暂不可用**。
  15. Longbridge用户已自行开户，待其提供AppKey/AppSecret/AccessKey三件套。
- **功能增强第六轮（2026-09-13，多模型路由骨架+成本优化）**：
  16. 新建 core/llm_routing.py：按任务复杂度分层。
      - PREMIUM_NODES={trader,risk,portfolio,conviction,manager}；开关 STOCKAI_DEBATE=on 时裁决节点先试 Cohere(command-r-08-2024, OpenAI兼容端点)，失败自动降级通义；默认 off 全程通义不烧 key。
      - 结果缓存：(node,user) 在 STOCKAI_CACHE_TTL=300 秒内重复调用直接返回，不重复耗 API。
      - 踩坑：core/llm.py 是文件，误建 core/llm/ 目录冲突，改平级模块 core/llm_routing.py。
  17. core/llm.py chat_json 接入：入口查缓存；premium+debate 先试 cohere_chat；通义成功结果写缓存。
  18. report.py 置信度门槛：confidence<40 红字提示"信号弱不据此操作建议观望"；<60 提示小仓试错。
  19. 全量回归 11/11 通过。
- **功能增强第七轮（2026-09-13，第二模型换智谱GLM，多模型辩论真启用）**：
  20. OpenAI充值放弃（国内无海外卡/ApplePay），key留着不用。
  21. bu自动化登录智谱 open.bigmodel.cn（手机15579102024），apikey/platform 新建key(stockai)，拿到 GLM key 存.env。
      实测 glm-4-flash 跑通（国内 trust_env=False 绕Clash），返回正常。
  22. routing.py 加 glm_chat（智谱glm-4-flash，免费耐用）；新增 premium_chat = GLM优先、Cohere兜底。
  23. llm.py premium路由改用 premium_chat。.env STOCKAI_DEBATE=on（真启用：裁决节点走GLM，分析节点走通义）。
  24. 全量回归 11/11 通过。重打exe offscreen启动验证。
- **合规化改造（2026-09-13，为开源GitHub）**：
  - 新增根目录文件：DISCLAIMER.md / PRIVACY.md / TERMS_OF_SERVICE.md / LICENSE(MIT) / README.md / .env.example / config.yaml.example。
  - config.py DISCLAIMER 改强措辞（未取得证监会资格、不构成投资建议、用户自担风险）。
  - prompts.py RULES 末尾加"合规红线"：只能客观数据描述，禁止投资建议/买卖/价格预测。
  - app/main.py 加 _show_disclaimer_if_first()：首启免责确认弹窗(QSettings记，点"我已阅读并理解"才继续)。
  - 新建 config_manager.py：.env 读 DEEPSEEK_API_KEY、缺失提示、mask_key脱敏不明文。修过ROOT算错一层bug。
  - .gitignore 补全（.env*.env.example白名单、dist/*.spec、data/、*.log）。
  - 全量回归 11/11 通过。
- **第二阶段：开源基建与社区生态（2026-09-13）**：
  - 新建：CONTRIBUTING.md / CODE_OF_CONDUCT.md(Contributor Covenant2.1) / SECURITY.md(48h响应) / .github/CODEOWNERS。
  - GitHub模板：.github/ISSUE_TEMPLATE/bug_report.md+feature_request.md、PULL_REQUEST_TEMPLATE.md、workflows/security_scan.yml(Gitleaks+Semgrep)。
  - config_manager.py 加 is_dev_mode()/update_channel()/APP_VERSION=0.1.0/check_update(urllib查GitHub Release只提示不自动下载)。
  - main_window.py：DEV_MODE=true时显示"开发者工具"页(清空缓存/看日志)，否则隐藏；"帮助"菜单加"意见反馈"(QDialog跳转GitHub Issues,不收集数据)+"检查更新"。
  - core/data/cache.py 补 clear_all()(清daily_bars+news)。
  - README.md 顶部醒目免责+详细步骤。
  - 全量回归 11/11 通过。
- **第三阶段：对话式三栏UI（2026-09-13）**：
  - 新建 app/ui/chat_tab.py：左200自选股QListWidget(add/del)、中QTextEdit对话流+QLineEdit、右PlotWidget(收盘+MA20)，三栏QSplitter联动。
  - 配色：BG#0D1117/FG#E6EDF3/涨#00C853/跌#FF1744。术语(RSI/MACD)用title悬浮解释；一句话总结卡片。
  - main_window.py：ChatTab作为首页"对话分析"标签，总12标签；不破坏原11标签。
  - main.py：首次启动3页新手引导(QStackedWidget,QSettings记welcome_shown)，在免责弹窗后。
  - selftest 7项全过、全量回归11/11通过。
