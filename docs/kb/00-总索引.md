# 自主记忆知识库 · 总索引与更新规则（StockAIPredictor 项目）

> 本库是本项目跨会话的长期记忆，固定路径：`D:\StockAIPredictor\docs\kb\`
> 每次新会话开场：先读本文件 + 计划书 `d:\craecode\magic\.trae\documents\虚拟股票预测程序_plan.md`，快速恢复全部上下文。
> 项目代码根目录：`D:\StockAIPredictor\`；venv：`D:\StockAIPredictor\venv\`（Python 3.14.7，依赖已装齐）。

## 一、强制更新规则（每次操作后执行）
每完成一次操作 / 试错 / 得出结论 / 推翻旧判断后，必须：
1. **追加流水**：在 `操作日志.md` 末尾按时间追加（做了什么、结果、关键数字/报错）。
2. **更新结论**：到对应主题文件写入新结论/新参数（去重、保持最新；旧错误结论移入"已推翻"区，不删除，避免重蹈）。
3. **记录误判**：凡"原以为 A、实际是 B"，写清【误判 → 证据 → 更正】三段。
4. **沉淀方法**：可复用的命令、排障框架、下载/打包套路写入 `03-踩坑与风险台账.md`。
5. 数字、路径、版本、接口名必须可追溯，不靠"应该/大概"；外部事实（模型名/价格/版本支持）标注核实日期。

## 二、文件导航
| 文件 | 内容 |
|---|---|
| `00-总索引.md` | 本文件：规则、导航、主线快照 |
| `01-开源项目调研.md` | GitHub 同类项目调研：星标/活跃度/架构/可复用点/需避开的坑 |
| `02-技术决策与选型.md` | 已锁定的技术栈、数据源清单、系统架构、项目目录规划 |
| `03-踩坑与风险台账.md` | 误判台账、已知坑（打包/数据源/LLM/合规）、可复用排障方法 |
| `操作日志.md` | 按时间的操作流水（只追加） |

## 三、当前主线快照（每次收尾更新）
- **总目标**：开发可独立运行的桌面 exe「虚拟股票预测程序」——联网取行情/资讯，AI 多智能体分析推演，输出炒股方案；覆盖 A股+美股+加密货币；定位学习研究+模拟盘。
- **用户已拍板（2026-09-10）**：① 市场=A股+美股+加密货币；② AI=DeepSeek 云端为主（key 已在 `.env`）；③ 界面=PyQt6 原生窗口；④ 定位=学习研究+模拟盘（不接实盘）。
- **技术基线（2026-09-10 核实）**：Python 3.14.7 + venv 依赖已装齐；DeepSeek 模型名已换代为 `deepseek-flash` / `deepseek-v4-pro`（旧 `deepseek-chat`/`deepseek-reasoner` 已于 2026-07-24 弃用）；PyInstaller 6.22 支持 Python 3.14 打包。
- **当前阶段（2026-09-11）**：**五期全部完成**：一期数据+8智能体+CLI → 二期 PyQt6 界面 → 三期模拟盘+决策反思 → 四期 RAG+相似形态 → 五期 PyInstaller 打包。**成品已交付：`dist/StockAIPredictor/`（onedir，双击即用，--selftest 冷库全链路 exit=0 已验）**。
- **六期增补（2026-09-11，本地离线 AI+蒸馏）**：新增 Ollama + Qwen2.5-7B-Instruct-Q4 本地引擎，AI 三后端 **deepseek/local/auto**（云端 402/断网自动切本地）；本机 RTX5060 实测 8 节点 26~34 秒/0 元/8/8 不降级。蒸馏闭环：真实推理→SFT 语料（teacher/student）、决策到期→good/bad 偏好标签、一键导出 LLaMA-Factory 训练集。关键坑：回环地址须绕系统代理（坑14）、Ollama daemon 需带代理重启才能 pull（坑15）、7B schema 漂移须在契约层归一化（坑16）。
- **七期增补（2026-09-11，教学资源蒸馏训练→全部完成）**：扩展蒸馏数据来源为网上开放许可教学资源（维基百科 CC-BY-SA/IMF/ECB/BIS/证监会投教），本地 7B 教师自蒸馏 Q&A（source=curriculum），零改动接 export 导出链。新增 core/training/{resources,curriculum,ollama_import}.py + app/ui/training_dialog.py + scripts/{bootstrap_venv_train.ps1,fetch_resources,train_offline,download_model_hftransfer} + training_configs/。改造 llm.py chat_json 加 raw 参数、distill.py export 加 sources 过滤、spec hiddenimports 补4模块。
  - **QLoRA 训练完成**：venv_train（Python 3.11.9 + torch 2.11.0+cu128 + LLaMA-Factory 0.9.5），Qwen2.5-3B-Instruct 基座，351 对 curriculum Q&A，4bit nf4 QLoRA，1 epoch 42 steps，train_loss 2.36→1.53，eval_loss 1.88，107s 完成。LoRA 合并 bfloat16 4 分片 5.9GB → ollama create stockai-qwen-curriculum（6.2GB）→ API 验证通过（"什么是市盈率"问答准确）→ .env 设为本地学生模型。关键坑：hf-mirror.com + hub 0.36 元数据校验失败→hf_transfer 直下（坑21）；dataset_dir 须在 YAML 非 CLI（坑23）。
- **八期（2026-09-11，策略回测引擎→完成）**：core/quant/backtest.py 自研零依赖日线回测（t+1 开盘成交防前视、CN/US/CRYPTO 真实费率滑点、整手碎股），4 策略注册表（rsi_reversion/ma_cross/macd_cross/buy_hold 基准），全套指标（年化/回撤/夏普/胜率/盈亏比/超额）；CLI scripts/run_backtest.py + GUI 第6标签"回测"（指标表+权益曲线vs基准+成交流水，结果落 data/backtests/）；11 个单测全绿，selftest exit=0，spec 无需改。实测茅台 RSI 25年 42笔胜率76%但跑输买入持有（均值回归不适合长牛股）。**关键认知：回测是后续多因子/MoE/FinBERT 的验证标尺，先建标尺再谈算法。**
- **待办/外部前置**：① DeepSeek 充值账号疑似充错（platform.deepseek.com 开放平台 ≠ 聊天网页版，官方接口实测余额 0）；② UI"带代理重启 Ollama"自动化、DPO 自动配对脚本待云端可用后补；③ 离线模型后续：增大数据集、多 epoch、DPO、7B 基座；④ 回测后续扩展（用户长清单已对照存档）：AI 信号历史回放、组合回测/归因、参数寻优、港股/ETF、自选股盯盘通知、多因子+市场状态路由、FinBERT、研报公告、报告导出、暗色三栏 UI——按阶段推进不铺开；⑤ 下次重打 exe 带回测标签；⑥ 用户会继续给优化建议。
- **最具参考价值的开源项目**：TradingAgents（104k★，多智能体+决策反思记忆+LangGraph）、ai-hedge-fund（63k★，基金/回测/模块化 alpha 模型）。

## 四、用户长期偏好（继承自历史记忆库）
- 软件装 D 盘；优先免费/高性价比；成品要能直接用。
- 排障/决策要给确定性结论，不要反复试单个开关；同一动作失败两次就换通道。
- 敏感信息（API key、密码）只存本机，不外传、不贴外部网页、不写进会上传的内容。
